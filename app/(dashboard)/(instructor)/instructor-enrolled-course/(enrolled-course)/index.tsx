import EnrolledCoursesClient from './EnrolledStudentsClient';

// This page shows the instructor's own enrolled courses (as a student)
// Not to be confused with students enrolled in the instructor's courses
const EnrolledCoursePage = () => {
  return <EnrolledCoursesClient />;
};

export default EnrolledCoursePage;
